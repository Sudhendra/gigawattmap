# Gigawatt Map API — Cloudflare Worker.
# The KV namespace caches upstream Finnhub responses (10 min TTL) so the free
# tier (60 req/min) comfortably covers all visitors, and so the upstream token
# never reaches the browser.
#
# To deploy you must first create the KV namespace and replace the `id` below:
#   wrangler kv:namespace create TICKERS_CACHE
#   wrangler kv:namespace create TICKERS_CACHE --preview
# Local `wrangler dev` uses Miniflare's in-memory KV automatically.
name = "gigawattmap-api"
main = "src/index.ts"
compatibility_date = "2024-11-12"
compatibility_flags = ["nodejs_compat"]
routes = [
  { pattern = "gigawattmap.com/*", zone_name = "gigawattmap.com" },
  { pattern = "www.gigawattmap.com/*", zone_name = "gigawattmap.com" },
]

[[kv_namespaces]]
binding = "TICKERS_CACHE"
id = "3be043bc06aa416999ef34035bdb3144"
preview_id = "ccc43f6b9b2f449c89eb630bc71707d2"

# `FINNHUB_TOKEN` is set as a secret, not in this file:
#   wrangler secret put FINNHUB_TOKEN

# Public download artifacts (datacenters.geojson, cables.geojson, manifest.json, ...).
# Populated by `opendc publish` from the data-pipeline. Bucket is shared with
# the PMTiles uploads (see data-pipeline/opendc/tiles/upload.py DEFAULT_BUCKET).
[[r2_buckets]]
binding = "ARTIFACTS"
bucket_name = "gigawattapp"
preview_bucket_name = "gigawattapp"

# Local-only override: when running `wrangler dev`, point this at the
# data-pipeline's `out/` directory so the API serves fresh pipeline output
# without needing `opendc publish` to have run.
[vars]
DEV_ARTIFACT_DIR = ""
